<?xml version="1.0" encoding="utf-8"?>
<!--
    Merged into the main manifest only for the `cloud` flavor. The `lan` flavor never links
    against Firebase at all, so none of this exists there.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <service
            android:name=".service.BeaconMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Beacon posts its own alert notification; never let FCM auto-generate one. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="beacon.alert" />
    </application>

</manifest>
